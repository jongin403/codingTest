// 프로그래머스
// 코딩테스트 연습 > 깊이/너비 우선 탐색(DFS/BFS) > 타겟 넘버

function main(){
    let answer = new Array();
    
    // 입력
    answer.push(solution(5,3));
    
    // 출력
    for(let i = 0; i < answer.length; i++){
        console.log(answer[i]);
    }
}

function solution(numbers, target) {
    let answer = 0;
    return answer;
}

main();